# Parsed transcript corpus (12MB, 96 files) — store via git-LFS so the eval set is frozen.
data/transcripts/*.json filter=lfs diff=lfs merge=lfs -text
data/transcripts/*.json !text !filter !merge !diff
