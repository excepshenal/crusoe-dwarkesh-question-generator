# Raw API dumps are reproducible bulk — don't commit. (Parsed transcripts ARE
# committed, via git-LFS; see .gitattributes — they freeze the eval corpus.)
data/raw/
data/scrape.log
# Generated outputs / eval runs (regenerable, bulky). SFT datasets ARE committed
# (sft/data/*.jsonl via git-LFS; see .gitattributes).
runs/
last_run.json
*.pyc
__pycache__/
.env
evals/*.log
